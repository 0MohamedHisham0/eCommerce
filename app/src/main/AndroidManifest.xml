<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dal4.ecommerce">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerce"
        tools:targetApi="donut">
        <activity
            android:name=".admin_ui.HomeAdminActivity"
            android:exported="true">
        </activity>
        <activity android:name=".ui.Search_Screen" />
        <activity android:name=".ui.ItemDetail" />
        <activity android:name=".ui.Category_Screen" />
        <activity android:name=".ui.Frag_Container" />
        <activity android:name=".authentication.SignIn_Admin_Screen" />
        <activity android:name=".authentication.SignIn_Screen" />
        <activity android:name=".authentication.SignUp_Screen" />

        <activity android:name=".ui.First_Screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>